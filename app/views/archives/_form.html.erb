<%= form_for [@group, @archive] do |f| %>
  <% if @archive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@archive.errors.count, "error") %> prohibited this archive from being saved:</h2>

      <ul>
      <% @archive.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h3>New Lift Record</h3>

  <div class="field">
    <%= f.label :lift_weight %><br>
    <%= f.number_field :lift_weight %>
  </div></br>

  <div class="field">
    <%= f.label :lift_name %><br>
    <%= f.select(:lift_name, ["Squat", "Deadlift", "Bench", "Overhead Press"]) %>
  </div></br>

  <div class="field">
    <%= f.label :video_link %><br>
    <%= f.text_field :video_link %>
  </div>

  </br>

  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
