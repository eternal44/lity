<p id="notice"><%= notice %></p>

<h1>Listing Groups</h1>

  <% @groups.each do |group| %>
    <div>
      <h2><%= link_to group.group_name, group %></h2>
      <p>Created by <%= group.user.first_name %></p>
    </div>
  <% end %>
<br>

<%= link_to 'New Group', new_group_path %>
