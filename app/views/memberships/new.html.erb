<%= form_for [@group, @membership] do |f| %>
  <% if @membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membership.errors.count, "error") %> prohibited this archive from being saved:</h2>

      <ul>
      <% @membership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if @memberships.present? %>
    <div class="pull-right">
      <h3>Members in the group</h3>
      <% @memberships.each do |membership| %>
        <%= "#{membership.user.first_name + " " + membership.user.last_name} " %></br>
      <% end %>
    </div>
  <% end %>


  <h3>Add Member to group</h3>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :group_role %><br>
    <%= f.text_field :group_role %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
